> Full database schema — all tables and their columns.

### Core Tables

**`company`**
```
id, 
name, 
slug, 
HQ, 
companySize, 
departments, 
logo,
goals, 
description,
industry, 
locations, 
standardTime, 
website, 
foundedYear, 
socialLinks,
verificationStatus, 
subscriptionPlan, 
subscriptionExpiresAt, 
createdAt, 
updatedAt
```

**`user`**
```
id, 
name, 
username, 
companyEmail, 
personalEmail, 
profile, 
companyId, 
position,
contactNumber, 
socialLinks, 
type (employee | contractor | freelancer),
workingSchedule, 
workingHours, 
department, 
joinedAt, 
country, 
standardTime,
isManager, 
managerId, 
status (active | inactive | suspended | on_leave),
timezone, 
createdAt, 
updatedAt
```

---

### Leave & Activity

**`leaveRequests`**
```
id, 
userId, 
companyId,
status (pending | approved | rejected | cancelled),
type (Half Day | Full Day | Multiple Days | Late Start | Early Leave),
category (Vacation | Sick Leave | Personal Leave | Maternity Leave | Paternity Leave),
reason, 
startDate, 
endDate, 
reviewedBy, 
reviewedAt, 
attachments, 
createdAt
```

**`userActivity`**
```
id, 
userId, 
companyId,
status (Working | Available | Break | Meeting | Away | Offline),
lastActiveAt, 
currentTaskId, 
updatedAt
```

---

### Analytics & Finance

**`analytics`**
```
id, 
userId, 
companyId, 
date, 
punchedInTime, 
punchedOutTime, 
hoursWorked,
isLate, 
lateByMinutes, 
numberOfBreaksTaken, 
totalBreakDuration,
keyboardAndMouseUsage, 
tasksCompletedToday, 
focusScore,
overtimeHours, 
earlyLeaveBy
```

**`userFinance`**
```
id, 
userId, 
companyId, 
payPerHour, 
billingCurrency, 
bankingCurrency,
bankingAccountNumber, 
bankingRoutingNumber, 
bankingProvider,
payThisMonth, 
payLastMonth, 
totalEarnings, 
stocksInCompany,
taxRate, 
deductions, 
bonuses,
paymentStatus (paid | pending | processing),
lastPaidAt, 
nextPayDate, 
payslips
```

---

### Tasks & Work

**`tasks`**
```
id, 
userId, 
companyId, 
projectId, 
title, 
description,
status (pending | in_progress | review_needed | completed | cancelled | blocked),
urgency (low | medium | high | critical),
difficulty (easy | medium | hard | expert),
dueDate, 
assignedTo, 
assignedBy, 
reviewedBy,
reviewStatus (pending | approved | rejected | revision_requested),
attachments, 
members, 
subTasks, 
comments, 
obstacles, 
points,
progress, 
tags, 
estimatedHours, 
actualHours, 
createdAt, 
modifiedAt
```

**`subTasks`**
```
id, 
taskId, 
title,
status (pending | in_progress | completed),
assignedTo, 
dueDate, 
createdAt, 
completedAt
```

**`userWorkedOnTask`**
```
id, 
userId, 
taskId, 
startedAt, 
endedAt, 
hoursWorked, 
sessionNotes
```

**`taskComments`**
```
id, 
taskId, 
userId, 
content, 
attachments, 
mentions,
edited, 
editedAt, 
createdAt
```

**`obstacles`**
```
id, 
taskId, 
reportedBy,
category (awaiting_design | awaiting_review | blocked_by_task | external_dependency | other),
reason, 
resolvedAt, 
isResolved, 
createdAt
```

---

### Projects

**`projects`**
```
id, 
companyId, 
name, 
slug, 
description,
status (planning | active | on_hold | completed | cancelled),
ownerId, 
members, 
startDate, 
endDate, 
budget, 
tags,
priority (low | medium | high),
visibility (public | private | team_only),
totalPoints, 
completionPercentage, 
createdAt, 
updatedAt
```

**`projectMembers`**
```
id, 
projectId, 
userId,
role (lead | contributor | reviewer | observer),
joinedAt
```

---

### Productivity

**`todo`**
```
id, 
userId, 
content,
status (pending | completed | skipped),
priority (low | medium | high),
date, 
dueTime, 
completedAt, 
createdAt
```

**`meetings`**
```
id, 
companyId, 
hostId, 
title, 
description,
type (one_on_one | team | company_wide),
scheduledAt, 
duration,
status (scheduled | ongoing | completed | cancelled),
participants, 
recordingUrl, 
summaryNotes, 
agenda, 
meetingLink, 
createdAt
```

**`documents`**
```
id, 
companyId, 
authorId, 
title, 
content,
type (wiki | policy | sop | template | note),
visibility (public | team | private),
lastEditedBy, 
tags, 
version, 
collaborators, 
createdAt, 
updatedAt
```

---

### HR & Org

**`departments`**
```
id, 
companyId, 
name, 
description, 
headId,
parentDepartmentId, 
memberCount, 
createdAt
```

**`announcements`**
```
id, 
companyId, 
authorId, 
title, 
content,
type (general | urgent | policy | event),
targetAudience (all | department | role),
isPinned, 
publishedAt, 
expiresAt, 
readBy, 
createdAt
```

**`performanceReviews`**
```
id, 
companyId, 
revieweeId, 
reviewerId,
cycle (Q1 | Q2 | Q3 | Q4 | Annual),
status (draft | submitted | acknowledged),
overallScore, 
strengths, 
improvements, 
goals,
managerNotes, 
selfAssessment, 
createdAt, 
submittedAt
```

**`performanceGoals`**
```
id, 
userId, 
companyId, 
reviewId, 
title, 
description,
type (okr | smart), 
targetDate, 
progress,
status (active | completed | missed),
createdAt
```

**`feedback360`**
```
id, 
revieweeId, 
reviewerId, 
companyId, 
reviewCycleId,
relationship (peer | manager | direct_report | self),
rating, 
comments, 
isAnonymous, 
submittedAt
```

---

### Gamification & Culture

**`teamLeaderboard`**
```
id, 
companyId, 
userId, 
points, 
rank,
projectsCompleted, 
projectsPending, 
tasksCompleted,
rewards (most_active | hard_problem_solver | helper | team_player | missed_deadlines | most_leave_requests | overdue_tasks),
streak, 
weeklyPoints, 
monthlyPoints, 
updatedAt
```

**`rewards`**
```
id, 
companyId, 
userId, 
title, 
description,
type (badge | bonus | certificate | shoutout),
awardedBy, 
awardedAt, 
isPublic
```

**`culturePosts`**
```
id, 
companyId, 
authorId,
type (spotlight | celebration | poll | update | event),
content, 
attachments, 
likes, 
comments,
isPinned, 
targetAudience, 
createdAt
```

**`moodCheckins`**
```
id, 
userId, 
companyId,
mood (great | good | neutral | low | burned_out),
note, 
isAnonymous, 
date, 
createdAt
```

**`polls`**
```
id, 
companyId, 
createdBy, 
question, 
options,
isAnonymous, 
allowMultiple, 
expiresAt, 
responses, 
createdAt
```

---

### Finance & Payroll

**`payroll`**
```
id, 
companyId, 
userId,
period (monthly | bi-weekly | weekly),
periodStart, 
periodEnd, 
basePay, 
overtime, 
bonus,
deductions, 
tax, 
netPay, 
currency,
status (draft | processed | paid),
paidAt, 
payslipUrl, 
createdAt
```

**`expenses`**
```
id, 
userId, 
companyId, 
title, 
amount, 
currency,
category (travel | equipment | software | meals | other),
receipt, 
status (pending | approved | rejected | reimbursed),
submittedAt, 
reviewedBy, 
reimbursedAt, 
notes
```

---

### Recruitment

**`jobPostings`**
```
id, 
companyId, 
title, 
description, 
departmentId,
type (full_time | part_time | contract | internship),
location, 
isRemote, 
salaryMin, 
salaryMax, 
salaryCurrency,
requirements, 
status (draft | open | closed | filled),
postedBy, 
postedAt, 
expiresAt
```

**`candidates`**
```
id, 
jobId, 
companyId, 
name, 
email, 
phone, 
resumeUrl,
coverLetter, 
portfolioUrl, 
linkedIn,
status (applied | screening | interview | offer | hired | rejected),
source (referral | linkedin | direct | agency),
appliedAt, 
assignedTo, 
notes
```

**`interviews`**
```
id, 
candidateId, 
jobId, 
companyId, 
interviewers, 
scheduledAt,
type (phone | video | on_site | technical | hr_round),
status (scheduled | completed | cancelled | no_show),
feedback, 
rating,
outcome (pass | fail | hold),
createdAt
```

---

### Notifications & Audit

**`notifications`**
```
id, 
userId, 
companyId,
type (task | mention | review | leave | payroll | system),
title, 
body, 
referenceId, 
referenceType, 
isRead, 
createdAt
```

**`auditLogs`**
```
id, 
companyId, 
actorId, 
action, 
targetType,
targetId, 
metadata, 
ipAddress, 
userAgent, 
createdAt
```

**`integrations`**
```
id, 
companyId,
provider (google_calendar | slack | github | jira | stripe),
accessToken, 
refreshToken, 
config, 
isActive, 
connectedAt, 
updatedAt
```

---

### Messaging

> The communication suite had no backing schema. These tables power the full messenger.

**`channels`**
```
id, 
companyId, 
name, 
description,
type (direct | group | project | announcement | public),
createdBy, 
members, 
projectId, 
isArchived,
lastMessageAt, 
createdAt, 
updatedAt
```

**`messages`**
```
id, 
channelId, 
senderId, 
companyId, 
content,
type (text | image | file | voice | video | system),
replyToId, 
attachments, 
mentions, 
isEdited,
editedAt, 
isDeleted, 
deletedAt, 
createdAt
```

**`messageReactions`**
```
id, 
messageId, 
userId, 
emoji, 
createdAt
```

**`messageReadReceipts`**
```
id, 
messageId, 
channelId, 
userId, 
readAt
```

---

### Calendar & Scheduling

> `calendarEvents` was missing entirely — meetings exist but general events, 
deadlines, 
and reminders had no table.

**`calendarEvents`**
```
id, 
companyId, 
createdBy, 
title, 
description,
type (meeting | reminder | deadline | holiday | ooo | event),
startAt, 
endAt, 
isAllDay, 
isRecurring,
recurrenceRule, 
location, 
meetingLink,
visibility (public | team | private),
attendees, 
color, 
linkedTaskId, 
linkedProjectId,
status (confirmed | tentative | cancelled),
createdAt, 
updatedAt
```

**`publicHolidays`**
```
id, 
country, 
region, 
name, 
date, 
year, 
isOptional
```

**`leaveBalances`**
```
id, 
userId, 
companyId, 
year,
vacationTotal, 
vacationUsed, 
vacationPending,
sickTotal, 
sickUsed, 
sickPending,
casualTotal, 
casualUsed, 
casualPending,
parentalTotal, 
parentalUsed,
compensatoryEarned, 
compensatoryUsed,
updatedAt
```

---

### Auth & Security

> Sessions, 
devices, 
and invitations had no tables — these are critical for auth, 
2FA, 
and onboarding flows.

**`sessions`**
```
id, 
userId, 
token, 
ipAddress, 
userAgent,
device, 
location, 
isActive, 
expiresAt, 
createdAt
```

**`userDevices`**
```
id, 
userId, 
deviceName, 
deviceType (desktop | mobile | tablet),
os, 
browser, 
ipAddress, 
isTrusted,
lastSeenAt, 
registeredAt
```

**`twoFactorAuth`**
```
id, 
userId, 
method (totp | sms | email),
secret, 
isEnabled, 
backupCodes,
lastUsedAt, 
createdAt
```

**`invitations`**
```
id, 
companyId, 
invitedBy, 
email, 
role,
department, 
token, 
status (pending | accepted | expired | revoked),
expiresAt, 
acceptedAt, 
createdAt
```

---

### Onboarding

> Users have onboarding flows but no way to track completion state.

**`onboardingTemplates`**
```
id, 
companyId, 
name, 
targetRole, 
targetDepartment,
steps, 
isDefault, 
createdBy, 
createdAt, 
updatedAt
```

**`onboardingProgress`**
```
id, 
userId, 
companyId, 
templateId,
completedSteps, 
totalSteps, 
currentStep,
status (not_started | in_progress | completed),
startedAt, 
completedAt
```

---

### Skills & Capacity

> The smart assignment engine references skills and capacity but neither had a table.

**`userSkills`**
```
id, 
userId, 
skill, 
proficiency (beginner | intermediate | advanced | expert),
yearsOfExperience, 
isVerified, 
verifiedBy, 
addedAt
```

**`workCapacity`**
```
id, 
userId, 
companyId, 
maxHoursPerDay, 
maxHoursPerWeek,
currentLoad, 
availableFrom, 
unavailableUntil,
updatedAt
```

---

### Tasks (Additions)

**`taskDependencies`**
```
id, 
taskId, 
dependsOnTaskId,
type (blocks | is_blocked_by | relates_to | duplicates),
createdBy, 
createdAt
```

**`projectMilestones`**
```
id, 
projectId, 
companyId, 
title, 
description,
dueDate, 
status (upcoming | achieved | missed),
linkedTasks, 
createdBy, 
achievedAt, 
createdAt
```

---

### Compensation & Benefits

> `userFinance` tracks current pay but has no history, 
and benefits had no dedicated table.

**`compensationHistory`**
```
id, 
userId, 
companyId, 
previousPay, 
newPay, 
currency,
changeReason (promotion | annual_review | adjustment | role_change),
effectiveDate, 
approvedBy, 
createdAt
```

**`benefits`**
```
id, 
companyId, 
name, 
description,
type (health | dental | vision | equity | wellness | learning | other),
provider, 
coverageDetails, 
eligibleRoles,
isActive, 
createdAt, 
updatedAt
```

**`benefitEnrollments`**
```
id, 
userId, 
companyId, 
benefitId,
status (enrolled | waived | pending | terminated),
enrolledAt, 
effectiveDate, 
terminatedAt
```

---

### Subscriptions & Billing

**`subscriptionPlans`**
```
id, 
name (free | pro | business | enterprise),
pricePerSeat, 
billingCycle (monthly | annual),
maxSeats, 
features, 
isActive, 
createdAt
```

**`companyBilling`**
```
id, 
companyId, 
planId, 
seats, 
billingEmail,
stripeCustomerId, 
stripeSubscriptionId,
status (active | past_due | cancelled | trialing),
currentPeriodStart, 
currentPeriodEnd,
cancelledAt, 
createdAt, 
updatedAt
```
